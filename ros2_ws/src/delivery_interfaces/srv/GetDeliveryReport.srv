# =============================================================================
# GetDeliveryReport.srv — 获取配送报告服务
# =============================================================================
# 用途：查询当前系统中所有配送任务的状态快照，用于监控和统计。
# 使用场景：
#   - 监控面板定期轮询，获取全局配送概况
#   - 调试时查看所有订单的当前状态
#   - 日志系统记录某一时刻的完整配送状态
# 服务端行为：
#   - 收集所有已知订单（包括排队中、执行中、已完成、已失败）的最新状态
#   - 以数组形式一次性返回，无分页机制
# 服务名称：/get_delivery_report
# 注意：请求中无需任何参数，服务端返回全量报告
# =============================================================================

# ---- 请求 (Request) ----
# （无请求参数 — 返回所有订单的状态报告）

---

# ---- 响应 (Response) ----

# 所有配送任务的状态报告列表
# 数组中的每个元素为一条 DeliveryStatus 消息，包含订单 ID、状态、进度等信息
# 数组按照订单提交时间排序（先提交的在前）
# 若系统中无任何订单，返回空数组
delivery_interfaces/DeliveryStatus[] reports
